version: "3.9"

services:
  openral-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
      args:
        ROS_DISTRO: jazzy
    image: openral-dev:latest
    container_name: openral-dev
    privileged: true
    network_mode: host
    # GPU passthrough. `privileged: true` + the /dev bind exposes the device
    # nodes but NOT the driver user-space libs, so CUDA stays invisible
    # without this — the dev shell was CPU-only while the deploy image
    # (docker/inference/Dockerfile.x86) had the GPU. Needs the NVIDIA
    # Container Toolkit on the host; `count: all` is a no-op reservation that
    # compose ignores gracefully on GPU-less hosts, so this stays portable.
    # CUDA itself comes from the torch wheels in .venv, which is why the base
    # image does not need to be an nvidia/cuda one.
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    volumes:
      - .:/workspace
      - /dev:/dev
      - /run/udev:/run/udev:ro
      - ${HOME}/.cache/huggingface:/root/.cache/huggingface
    environment:
      - DISPLAY=${DISPLAY:-:0}
      - ROS_DOMAIN_ID=${ROS_DOMAIN_ID:-0}
      # otelcol fans out: traces -> jaeger, metrics -> prometheus.
      # Jaeger alone returns StatusCode.UNIMPLEMENTED on /v1/metrics,
      # so we point the SDK at the collector by default.
      - OTEL_EXPORTER_OTLP_ENDPOINT=${OTEL_EXPORTER_OTLP_ENDPOINT:-http://otelcol:4317}
    stdin_open: true
    tty: true
    command: bash

  otelcol:
    image: otel/opentelemetry-collector-contrib:0.108.0
    container_name: openral-otelcol
    command: ["--config=/etc/otelcol/config.yaml"]
    volumes:
      - ./docker/otelcol/config.yaml:/etc/otelcol/config.yaml:ro
    ports:
      - "4317:4317"     # OTLP gRPC ingress (host-visible)
      - "4318:4318"     # OTLP HTTP ingress (host-visible)
      - "8889:8889"     # Prometheus scrape endpoint
      - "13133:13133"   # health_check
    depends_on:
      - jaeger

  jaeger:
    image: jaegertracing/all-in-one:latest
    container_name: openral-jaeger
    ports:
      - "16686:16686"   # Jaeger UI (host-visible)
      # OTLP ports stay internal to the compose network: only otelcol
      # talks to jaeger directly, so we don't publish 4317/4318 here.
    expose:
      - "14317"         # internal OTLP/gRPC (forwarded from otelcol)
    environment:
      - COLLECTOR_OTLP_ENABLED=true
      - COLLECTOR_OTLP_GRPC_HOST_PORT=:14317

  prometheus:
    image: prom/prometheus:v2.55.0
    container_name: openral-prometheus
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.retention.time=2h
    volumes:
      - ./docker/otelcol/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      - "9090:9090"     # Prometheus UI
    depends_on:
      - otelcol
